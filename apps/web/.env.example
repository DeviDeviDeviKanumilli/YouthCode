# Local API mode (Vite dev server proxies /api -> http://127.0.0.1:8000)
VITE_API_BASE_URL=/api

# Reviewer UUID from `npm run sync-api` or apps/api demo script output
# VITE_REQUESTER_ID=

# Optional bearer token when POST /auth/token is available
# VITE_API_TOKEN=
